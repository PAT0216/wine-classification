services:
  jupyter-notebook:
    build:
      context: .
      dockerfile: Dockerfile
    image: beardw/wine-classification:f602e53
    ports:
      - "8888:8888"
    volumes:
      - .:/home/jovyan
    deploy:
      resources:
        limits:
          memory: 5G  
    platform: linux/amd64
